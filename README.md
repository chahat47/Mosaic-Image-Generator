# Mosaic-Image-Generator
A Python desktop app that reconstructs any image as a grid of smaller tile images, each matched by average color. Features a tkinter GUI, built-in tile downloader, and a NumPy + Pillow backend for fast image processing and export.
